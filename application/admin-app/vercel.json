{
  "name": "e-punch-admin-app",
  "version": 2,
  "buildCommand": "cd ../common-core && yarn build && cd ../common-ui && yarn build && cd ../admin-app && yarn build",
  "outputDirectory": "dist",
  "installCommand": "cd .. && yarn install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
} 