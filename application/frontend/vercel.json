{
  "name": "e-punch-frontend",
  "version": 2,
  "buildCommand": "cd ../common && yarn build && cd ../frontend && yarn build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
} 