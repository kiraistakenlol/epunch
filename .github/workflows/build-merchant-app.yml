name: Build Merchant App

on:
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  GITHUB_USER: kiraistakenlol

jobs:
  build-merchant-app:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push merchant-app
        uses: docker/build-push-action@v5
        with:
          context: ./application
          file: ./application/merchant-app/Dockerfile
          platforms: linux/amd64
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.GITHUB_USER }}/epunch-merchant-app:latest
          build-args: |
            VITE_API_URL=${{ vars.VITE_API_URL }}
            VITE_USER_APP_URL=${{ vars.VITE_USER_APP_URL }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
